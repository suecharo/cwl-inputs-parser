version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile-dev
    image: cwl-inputs-parser-dev
    container_name: cwl-inputs-parser-dev
    volumes:
      - ${PWD}:/app
    working_dir: /app
